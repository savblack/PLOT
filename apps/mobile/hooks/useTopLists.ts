// Re-exported from the shared @plot/core package. ListType is kept here for screens that type against it.
export { useTopLists } from '@plot/core/useTopLists.js';

export type ListType = 'movies' | 'tv';
