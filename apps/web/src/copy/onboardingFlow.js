// The onboarding strings are shared with the mobile app, so they live in
// @plot/core/copy. Re-exported here so src/copy/ stays the one place the web
// app — and the Storybook Content page — looks for copy.
export { ONBOARDING_FLOW, SEED_LIKE_TARGET } from '@plot/core/copy/onboardingFlow.js';
