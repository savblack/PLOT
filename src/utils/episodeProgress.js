// Moved to @plot/core/episodeProgress.js (shared, platform-agnostic core).
// Re-export keeps existing `../utils/episodeProgress` import sites working.
export * from '@plot/core/episodeProgress.js';
