// Visible to everyone; usable by supporters. Server-side enforcement lives in
// the media-sync / trakt-sync edge functions (403 supporter_required).
export const SHOW_MEDIA_SYNC_INTEGRATIONS = true;
