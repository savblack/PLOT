// Now sourced from the shared @plot/core package.
// The AsyncStorage session config moved into lib/configureCore.ts (the storage
// seam), injected via configure() at app startup.
export * from '@plot/core/supabase.js';
