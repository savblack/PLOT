name: Watchlist availability alerts

on:
  # Paused until there's enough traffic to justify it — re-enable by
  # uncommenting the schedule below. workflow_dispatch still allows a manual run.
  # schedule:
  #   - cron: '15 8 * * *' # Daily; the function compares current regional availability to delivered alerts.
  workflow_dispatch:

permissions:
  contents: read

jobs:
  send-alerts:
    runs-on: ubuntu-latest
    steps:
      - name: Check and deliver availability alerts
        run: |
          curl --fail --silent --show-error --request POST "$SUPABASE_URL/functions/v1/watchlist-availability-alerts" \
            --header "x-cron-secret: $AVAILABILITY_ALERTS_CRON_SECRET"
        env:
          # See for-you-recompute.yml: secrets.SUPABASE_URL does not exist in
          # this repo (only VITE_SUPABASE_URL and SUPABASE_DB_URL do), so this
          # resolved to an empty string and curl failed before ever reaching
          # Supabase. Currently dormant (schedule paused above), but fixed so
          # re-enabling it doesn't reintroduce the same failure.
          SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          AVAILABILITY_ALERTS_CRON_SECRET: ${{ secrets.AVAILABILITY_ALERTS_CRON_SECRET }}
